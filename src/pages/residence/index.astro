---
import { getEntry } from 'astro:content';
import SectionPage from '../../layouts/SectionPage.astro';

const entry = await getEntry('sections', 'residence');
if (!entry) throw new Error('Missing section content: src/content/sections/residence.md');
---
<SectionPage entry={entry} />
