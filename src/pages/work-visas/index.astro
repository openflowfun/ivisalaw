---
import { getEntry } from 'astro:content';
import SectionPage from '../../layouts/SectionPage.astro';

const entry = await getEntry('sections', 'work-visas');
if (!entry) throw new Error('Missing section content: src/content/sections/work-visas.md');
---
<SectionPage entry={entry} />
